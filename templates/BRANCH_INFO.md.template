# Git Branch Configuration

This project uses **{{defaultBranch}}** as the default branch.

## Branch Strategy

**Default Branch:** `{{defaultBranch}}`

This branch name was {{detectionMethod}}.

## CI/CD Integration

When setting up CI/CD workflows, ensure they target the correct branch:

```yaml
on:
  push:
    branches:
      - {{defaultBranch}}
  pull_request:
    branches:
      - {{defaultBranch}}
```

## Feature Branch Workflow

```bash
# Create feature branch
git checkout -b feat/my-feature

# Work on changes
git add .
git commit -m "feat: add new feature"

# Push and create PR
git push -u origin feat/my-feature

# After review, merge to {{defaultBranch}}
```
